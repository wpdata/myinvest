from setuptools import setup, find_packages

setup(
    name="investapp",
    version="0.1.0",
    description="Streamlit UI for MyInvest",
    packages=find_packages(),
    python_requires=">=3.10",
)
